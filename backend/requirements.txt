# Backend dependencies. Requires Python 3.11 (numpy, scipy and scikit-image
# below all declare requires-python >=3.11). Verified on 3.11.0.
#
#     pip install -r backend/requirements.txt
#
# torch is NOT listed here on purpose - the right wheel depends on your CUDA
# version, and PyPI's default is CPU-only. Install it first, separately. Setup
# steps, including the model-weight download, are in the README.

# --- vision pipeline --------------------------------------------------------
ultralytics==8.4.90
opencv-python==4.11.0.86
numpy==2.4.6
scipy==1.17.1
Pillow==12.3.0
lap==0.5.13

# --- ViTPose++ keypoint refinement -----------------------------------------
transformers==5.14.1
accelerate==1.14.0

# --- WiLoR hand detection ---------------------------------------------------
# Weights are not pip-installable: python backend/fetch_wilor.py
timm==1.0.28
einops==0.8.2
smplx==0.1.28
pytorch-lightning==2.6.5
yacs==0.1.8
scikit-image==0.26.0
dill==0.4.1

# --- web / IO ---------------------------------------------------------------
websockets==11.0.3
aiohttp==3.9.3
requests==2.34.2

# --- optional ---------------------------------------------------------------
# Better QR decoding at distance, but needs the zbar DLL on Windows. main.py
# falls back to OpenCV's detector when absent.
# pyzbar==0.1.9
